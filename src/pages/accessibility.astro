---
import { getEntry } from "astro:content";
import LegalPage from "@/components/pages/LegalPage.astro";

const entry = await getEntry("legal", "de/accessibility");
if (!entry) throw new Error("Missing content/legal/de/accessibility.md");
---

<LegalPage entry={entry} locale="de" ref="accessibility" />
