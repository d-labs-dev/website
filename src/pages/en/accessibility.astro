---
import { getEntry } from "astro:content";
import LegalPage from "@/components/pages/LegalPage.astro";

const entry = await getEntry("legal", "en/accessibility");
if (!entry) throw new Error("Missing content/legal/en/accessibility.md");
---

<LegalPage entry={entry} locale="en" ref="accessibility" />
