---
import { entryPaths } from "@/lib/entry-routes";
import MethodPage from "@/components/pages/MethodPage.astro";

export const getStaticPaths = () => entryPaths("methods", "en");

const { entry, counterpartSlug } = Astro.props;
---

<MethodPage entry={entry} counterpartSlug={counterpartSlug} />
