---
import { getEntry } from "astro:content";
import LegalPage from "@/components/pages/LegalPage.astro";

const entry = await getEntry("legal", "en/imprint");
if (!entry) throw new Error("Missing content/legal/en/imprint.md");
---

<LegalPage entry={entry} locale="en" ref="imprint" />
